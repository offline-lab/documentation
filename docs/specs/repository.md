# Repository → Index contract

**Retired (2026-07-05).** The thing this page described is now an **index**
(ADR-0033/0037), and its specification lives in the
**[Index contract](index-contract.md)** — layout, signed manifest and
catalogs, the trust model (DDIs carry trust; indexes carry discovery),
subscribe + import, versioning and anti-rollback.

This stub remains only so old links don't break. Do not extend this page.
